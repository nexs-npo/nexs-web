---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/Header';
import BottomNav from '@/components/BottomNav';
---

<BaseLayout>
  <div class="min-h-screen bg-gray-50 flex flex-col">
    <!-- Header (React Island) -->
    <Header title="Home" client:load />

    <!-- Main Content -->
    <main class="flex-1 pt-24 pb-32 px-5 max-w-md mx-auto w-full">
      <div class="animate-fade-in-up">
        <!-- Status Section -->
        <section class="mb-12 mt-6">
          <div class="grid grid-cols-2 gap-4">
            <div class="bg-white rounded-xl p-5 border border-gray-100 shadow-sm">
              <p class="text-xs text-gray-500 font-bold uppercase mb-2">Active</p>
              <p class="text-2xl font-bold text-gray-900">2</p>
              <p class="text-xs text-gray-500 mt-1">Projects</p>
            </div>
            <div class="bg-white rounded-xl p-5 border border-gray-100 shadow-sm">
              <p class="text-xs text-gray-500 font-bold uppercase mb-2">Open</p>
              <p class="text-2xl font-bold text-gray-900">100%</p>
              <p class="text-xs text-gray-500 mt-1">Source Code</p>
            </div>
          </div>
        </section>

        <!-- Philosophy Section -->
        <section class="mb-12">
          <h2 class="text-sm font-bold text-gray-900 mb-4">Core Values</h2>
          <div class="space-y-3">
            <div class="bg-white rounded-xl p-4 border border-gray-100 shadow-sm">
              <p class="font-bold text-sm text-gray-900 mb-1">
                1. 安全と信頼
              </p>
              <p class="text-xs text-gray-600 leading-relaxed">
                Zero PII Strategy - 個人情報を自宅サーバーに保存しない
              </p>
            </div>
            <div class="bg-white rounded-xl p-4 border border-gray-100 shadow-sm">
              <p class="font-bold text-sm text-gray-900 mb-1">
                2. Open Source
              </p>
              <p class="text-xs text-gray-600 leading-relaxed">
                コードもまた、社会への還元物である
              </p>
            </div>
            <div class="bg-white rounded-xl p-4 border border-gray-100 shadow-sm">
              <p class="font-bold text-sm text-gray-900 mb-1">
                3. UX (Humans & AI)
              </p>
              <p class="text-xs text-gray-600 leading-relaxed">
                人間とAI、双方にとっての読みやすさを設計
              </p>
            </div>
          </div>
        </section>

        <!-- CTA Section -->
        <section class="text-center">
          <p class="text-xs text-gray-500 mb-4">
            このサイトは実験中です。フィードバックをお待ちしています。
          </p>
          <a
            href="mailto:info@nexs.jp"
            class="inline-flex items-center gap-2 text-xs font-bold text-gray-900 border-b border-gray-300 pb-0.5 hover:border-black transition-colors"
          >
            お問い合わせ →
          </a>
        </section>
      </div>
    </main>

    <!-- Bottom Navigation (React Island) -->
    <BottomNav activeTab="home" client:load />
  </div>
</BaseLayout>
